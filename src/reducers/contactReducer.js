// import {GET_CONTACTS} from '../actions/types'

// svaki reducer ima svoj initial state
const initialState = {
  //  test: 'TEST'
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
